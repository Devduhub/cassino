// Ad placeholder - keeps element permanently so adblock detection passes
var e = document.createElement('div');
e.id = 'MWUOvoYpNAmf';
e.style.display = 'none';
document.body.appendChild(e);
// Element is NOT removed so the game engine detects ads as "not blocked"
